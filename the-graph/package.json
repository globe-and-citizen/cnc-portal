{
  "name": "test-the-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ test-the-graph",
    "create-local": "graph create --node http://localhost:8020/ test-the-graph",
    "remove-local": "graph remove --node http://localhost:8020/ test-the-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test-the-graph",
    "test": "graph test",
    "git:ignore-locally": "git update-index --assume-unchanged subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.96.0",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
