meta {
  name: Auth - Missing Token
  type: http
  seq: 4
}

get {
  url: {{host}}/auth/token
  body: none
  auth: none
}

tests {
  test("should return 401 status when token is missing", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
