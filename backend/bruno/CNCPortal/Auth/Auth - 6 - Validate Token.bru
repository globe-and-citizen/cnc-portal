meta {
  name: Auth - 6 - Validate Token
  type: http
  seq: 6
}

get {
  url: {{host}}/auth/token
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("should return 200 status for valid token", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
