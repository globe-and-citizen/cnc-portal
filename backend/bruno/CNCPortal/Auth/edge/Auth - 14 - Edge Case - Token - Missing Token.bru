meta {
  name: Auth - 14 - Edge Case - Token - Missing Token
  type: http
  seq: 14
}

get {
  url: {{host}}/auth/token
  body: none
  auth: none
}

tests {
  test("should return 401 status when token is missing", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
