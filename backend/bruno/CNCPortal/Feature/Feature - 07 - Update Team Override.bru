meta {
  name: Feature - 07 - Update Team Override
  type: http
  seq: 7
}

put {
  url: {{host}}/admin/features/{{testFeatureName}}/teams/{{testTeamId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}


body:json {
  {
    "status": "beta"
  }
}

tests {
  test("should return 200 status", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should return updated override", function() {
    expect(res.body).to.be.an('object');
    expect(res.body.status).to.equal('beta');
  });

  test("log updated override", function() {
    console.log("Updated override:", JSON.stringify(res.body, null, 2));
  });
}
