meta {
  name: Feature - 01 - List All Features
  type: http
  seq: 1
}

get {
  url: {{host}}/admin/features
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}


tests {
  test("should return 200 status", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("should return features array", function() {
    expect(res.body).to.be.an('array');
  });

}
